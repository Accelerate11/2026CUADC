# Seed 4104 sample

Compact outputs of a complete randomized coverage run:

- `generated_scene.yaml`: random scene and evaluator truth.
- `observations.json`: raw C++ output; `state_machine_has_ground_truth` is false.
- `result.json`: post-mission evaluation.

Result: 14/14 coverage waypoints, three hazardous buckets correctly detected and classified, two empty buckets without false positives, 5/5 pass.

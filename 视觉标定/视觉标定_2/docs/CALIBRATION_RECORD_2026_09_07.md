# 标定记录 2026-09-07

当前采用版本：

```text
P1 / SERVO9  FLU = [+0.0260, -0.0650, -0.3200]
P2 / SERVO10 FLU = [+0.0109, +0.0720, -0.3200]
```

相机：

```text
camera_offset_body_m = [0,0,-0.35]
camera_xy_calib_bias_m = [+0.047805,-0.014577]
R optical->FLU =
[-0.971358, +0.237622, 0
 +0.237622, +0.971358, 0
  0,         0,       -1]
```

正式投放高度保持 1.3 m。

标定演进摘要：

1. 先在较低静态高度用 P1/P2 两点估计相机平面 yaw + XY 偏移；
2. 到 1.3 m 正式工作高度复核；
3. P1 在当前相机外参下已较准；
4. P2 采用单独机械投放口修正，避免为了 P2 破坏 P1 已正确的全局相机外参；
5. 当前 P2 `[+0.0109,+0.0720]` 为打包时采用值，后续仍可用 `sample_payload_xy.py` 继续复核。

# License

This public-clean workspace intentionally does **not** select a new license on the author's behalf.
Before publishing the repository, replace this file with the license you want to use and make the
`<license>` field in `ros2_ws/src/vision_servo_calibration/package.xml` match it.

The original detector/model stack has been removed from this public workspace, so no model or
Ultralytics license is bundled here.
